import { INTERNAL_runWithContext } from '../context.js';
export function contextMiddleware() {
    return (c, next)=>{
        const req = c.req.raw;
        return INTERNAL_runWithContext(req, next);
    };
}
export function rscMiddleware({ processRequest }) {
    return async (c, next)=>{
        const req = c.req.raw;
        const res = await processRequest(req);
        if (res) {
            c.res = res;
            return;
        }
        await next();
    };
}
export function middlewareRunner(middlewareModules) {
    let handlersPromise;
    return async (c, next)=>{
        if (!handlersPromise) {
            handlersPromise = Promise.all(Object.values(middlewareModules).map((m)=>m().then((mod)=>mod.default())));
        }
        const handlers = await handlersPromise;
        const run = async (index)=>{
            const handler = handlers[index];
            if (handler) {
                await handler(c, ()=>run(index + 1));
            } else {
                await next();
            }
        };
        await run(0);
    };
}

//# sourceMappingURL=middleware.js.map